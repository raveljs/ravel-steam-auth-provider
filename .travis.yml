language: node_js
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
node_js:
  - "8"
  - "9"
before_install:
  - $CXX --version
  - npm install
after_script:
  - codeclimate-test-reporter < reports/lcov.info
